# 可靠分片上传服务（Chunked Upload Service）

## 1. 项目目标与工程约束

开发一个生产型、可靠的分片上传服务：大文件通过 HTTP 分片上传，支持断点续传、分片与整体双重校验、服务端流式合并、过期会话回收以及文件元数据查询与下载。面向 Web 与移动端的大文件上传场景（视频、安装包、数据集、日志归档、镜像等），服务端把上传从"一次性大请求"改造成"可恢复、可校验、可审计的分片流"。

指导提示词：

> 用 Go 开发可靠的分片上传服务，支持断点续传、校验、合并、过期回收和文件元数据查询；按分层架构实现不少于 15 个文件，使用持久化和迁移，提供 API、后台清理、测试及完整验证。

首轮工程约束：

- 非测试 Go 文件 21–24 个，设计目标固定 **22 个**（同时满足"不少于 15 个"与"大于 20、小于 25"两项要求）；
- 非测试 Go 代码 **2001–2199 行**（测试代码、迁移 SQL、前端静态文件、README 不计入）；
- 必须包含数据库与版本化迁移、分层实现、HTTP API、后台清理任务、简单前端界面、测试与运行校验，不按轻量 Demo 交付。

## 2. 核心功能与业务逻辑扩写

### 2.1 总体流程

1. 客户端 `POST /api/v1/uploads` 创建上传会话：提交文件名、MIME、总大小、分片大小、期望校验和（可选）与幂等键 `upload_key`。
2. 服务端分配 `session_id`，按策略校验文件与分片参数，创建存储目录，返回会话详情（含分片总数与过期时间）。
3. 客户端按 `index` 逐个 `PUT` 分片（允许乱序、允许间隙）；服务端逐片落盘、记录元数据并计算分片 SHA-256。
4. 客户端可 `GET` 会话详情获取已上传分片位图与缺失分片列表，跳过已上传分片实现**断点续传**（刷新页面、换网络、服务重启均无损）。
5. 全部就绪后 `POST complete`；服务端校验分片完整性 → 按序流式合并 → 计算整体 SHA-256 → 与客户端期望值比对 → 生成文件记录，会话进入 `completed`。
6. 通过 `GET /api/v1/files` 查询元数据、`GET /api/v1/files/{id}/content` 断点下载。
7. 后台 `reaper` 周期扫描：清理过期未完成会话（分片 + 存储目录）、孤儿分片与残留合并临时文件，释放磁盘。

### 2.2 上传会话状态机

```
created -> uploading -> merging -> completed
   |          |            |
   +----------+------------+--> failed（原因可修复，允许重试 complete）
   |          |
   +----------+--> aborted（客户端主动取消）
   |          |
   +----------+--> expired（TTL 到期，由 reaper 标记并清理）
```

- `created`：会话已建，元数据入库，存储目录已分配；允许写入分片。
- `uploading`：已写入首个分片；分片可乱序、可带间隙写入。
- `merging`：收到 complete 后进入；合并期间拒绝新分片，重复 complete 幂等返回"进行中"。
- `completed`：合并成功且文件记录已入库；会话只读，位图接口仍可查询但不可写。
- `failed`：合并失败（缺分片、分片校验失败、磁盘错误）；写入 `failure_reason`，修复后允许重试 complete。
- `aborted`：客户端 `DELETE` 主动终止；立即或由 reaper 清理分片与目录。
- `expired`：超过 `expired_at` 仍未完成；reaper 标记并清理。

状态迁移只能在应用服务的事务内完成，每次迁移写入 `upload_session_events` 审计记录（from/to/reason/时间），保证可追溯。

### 2.3 分片规则与校验

- 分片下标从 0 开始，共 `N = ceil(total_size / chunk_size)` 片。
- 除最后一个分片外，其余分片必须恰好等于 `chunk_size`；最后一个分片可小于等于 `chunk_size`（服务端按请求头与记录双重校验）。
- 单分片大小与文件总大小受 `UploadPolicy` 约束（默认分片 1MB–64MB，文件 ≤ 20GB，可配置）。
- 每个分片请求必须携带 `X-Chunk-Index`、`X-Chunk-Size`（或等价字段），服务端以请求体的实际 `Content-Length` 为准并校验一致性。
- 每片计算 SHA-256 入库；同一 `index` 重复上传：哈希相同则幂等忽略（返回 200 已存在），哈希不同则覆盖并更新记录（用于重传损坏分片）。
- 允许乱序与间隙写入（断点续传的基础），但 `complete` 时要求 `0..N-1` 全齐且每个分片哈希与入库记录一致。
- 整体校验：若客户端提供 `expected_checksum`（SHA-256），合并完成后比对；不一致返回 409，保留合并临时文件允许重新 complete，最终由 reaper 兜底清理。

### 2.4 断点续传

- `GET /api/v1/uploads/{id}` 返回：分片总数、`chunk_size`、已上传分片下标数组（或位图）、缺失分片列表、已传字节/剩余字节、过期时间、当前状态。
- 客户端据此只补传缺失分片，不重复上传；已传分片按哈希幂等，天然支持并发分片重试。
- 会话状态持久化于 PostgreSQL，字节落盘到按 `session_id` 隔离的存储目录：服务重启、多实例切换、客户端刷新均无损续传。
- 续传同样覆盖 `failed` 状态：合并失败的临时文件可复用，避免整文件重新上传。

### 2.5 校验与合并（Merge）

- 触发：`POST /api/v1/uploads/{id}/complete`，幂等——重复调用返回当前状态（进行中/已完成/失败原因）。
- 同步合并流程：
  1. `SELECT ... FOR UPDATE` 锁定会话行，防并发 complete 双跑；
  2. 校验状态合法且 `0..N-1` 分片齐全；
  3. 按 `index` 升序流式读取分片文件，边拼边算 SHA-256，写入同目录 `.part` 临时文件；
  4. `fsync` 落盘后原子改名进 final 存储区；
  5. 比对整体校验和（如提供期望值）；
  6. 同一事务内：写 `file_records`、会话置 `completed`、保留分片记录用于审计。
- 任一环节失败：会话置 `failed` 并写 `failure_reason`；临时文件保留供重试，超时由 reaper 清理。
- 异步模式：`complete` 仅将任务入队（状态置 `merging` 并记录 `claimed_at`），后台 `merger` 领取执行，避免大文件合并阻塞请求；两种模式由配置切换。

### 2.6 过期回收（Reaper）

- 周期扫描（默认 60s，可配置），处理两类目标：
  - 未完成会话：`expired_at < now` 且状态为 `created/uploading` 的会话；
  - 悬挂会话：`merging/failed` 状态超过回收宽限期（默认 24h）的会话。
- 动作：删除存储目录（分片 + 临时 + 半成品）→ 会话置 `expired` → 写回收审计事件 → 上报释放字节指标。
- 孤儿清理：扫描存储根目录下不存在对应会话记录的分片目录，幂等删除（以文件系统 mtime 兜底防误删）。
- 多实例安全：`pg_try_advisory_lock`（或 `claimed_at` 条件更新）抢占扫描，避免多副本重复回收。
- 指标：本轮扫描数、回收数、释放字节、扫描耗时、扫描错误。

### 2.7 文件元数据查询

- `GET /api/v1/files`：按名称前缀、MIME、大小区间、上传时间区间、上传者筛选，游标分页返回元数据列表。
- `GET /api/v1/files/{id}`：名称、大小、SHA-256、分片数、来源会话、MIME、上传时间、下载次数。
- `GET /api/v1/files/{id}/content`：流式下载，支持 `Range` 与 `ETag`/`If-None-Match`（大文件断点下载）。
- `DELETE /api/v1/files/{id}`：删除文件记录与物理文件（可配置软删 + 保留期，保留期内仅标记 `deleted_at`）。

### 2.8 可靠性设计

- 所有元数据持久化于 PostgreSQL，字节存储抽象为 `port/storage` 接口（本地磁盘/对象存储可替换）。
- 所有写操作幂等：重复 create 返回既有会话；重复分片按哈希幂等；重复 complete 幂等；reaper 回收幂等。
- 合并的事务边界：物理文件操作与数据库事务通过状态机保证最终一致——任何一步失败都回到可重试的 `failed`，不留半成品为"成功"。
- 请求级：`X-Request-ID` 贯穿日志；请求体大小受策略约束；分片写入受 `Content-Length` 与策略双重限制。
- 优雅停机：worker 收到 SIGTERM 后停止领取新任务并等待当前任务结束；HTTP 服务等待在途请求完成。

## 3. 领域模型

| 模型 | 关键字段 | 说明 |
| --- | --- | --- |
| UploadSession | id、upload_key、file_name、mime_type、total_size、chunk_size、chunk_count、status、checksum_algorithm、expected_checksum、actual_checksum、storage_root、uploaded_by、created_at、updated_at、expired_at、completed_at、failure_reason | 一次上传会话；`upload_key` 唯一用于幂等 create |
| ChunkRecord | session_id、chunk_index、size、sha256、stored_path、uploaded_at | 分片元数据；(session_id, chunk_index) 联合主键 |
| FileRecord | id、session_id、name、size、sha256、chunk_count、mime_type、status、download_count、uploaded_at、deleted_at | 合并完成后的文件元数据，供查询与下载 |
| UploadSessionEvent | id、session_id、from_status、to_status、reason、created_at | 会话状态迁移审计 |
| UploadPolicy | min_chunk_size、max_chunk_size、max_file_size、session_ttl、reap_grace、allowed_mime、storage_root | 运行期策略（配置驱动，可入库便于变更） |

## 4. 分层架构

```
前端(web/static) -> HTTP/API(transport/http) -> 应用服务(app) -> 领域模型(domain)
                                                        ^
                                        端口接口(port) <--+
                                                        v
                              store/postgres（持久化） + store/disk（字节存储）
                              worker（reaper 过期回收 / merger 异步合并）
```

目录职责：

| 包 | 职责 |
| --- | --- |
| cmd/uploader | 入口：命令分发（serve / migrate / version）、依赖组装 |
| internal/config | 配置加载（环境变量优先 + 可选 YAML） |
| internal/domain | 会话状态机、分片规则、校验和、策略等纯规则，零框架依赖 |
| internal/port | 仓储、字节存储、时钟与 ID 生成接口 |
| internal/app | 创建会话、写分片、续传查询、合并编排、文件管理用例 |
| internal/store/postgres | 连接池、事务助手、三张核心表持久化 |
| internal/store/disk | 本地磁盘对象存储实现 |
| internal/worker | reaper（过期回收）、merger（异步合并） |
| internal/transport/http | 路由、中间件、DTO、错误映射、`go:embed` 前端静态资源 |

分层规则：app 不写 SQL，domain 不依赖任何框架，store 只实现 port 接口，worker 只通过 app/port 工作；HTTP 层只做协议转换。

## 5. 数据库与迁移

PostgreSQL 15+，时间统一 UTC。迁移文件位于 `migrations/`：

- `0001_init.up.sql` / `0001_init.down.sql`：建表 `upload_sessions`、`chunk_records`、`file_records`、`upload_session_events`。
- `0002_indexes.up.sql` / `0002_indexes.down.sql`：扫描与查询索引。

表结构与关键约束：

| 表 | 关键列与约束 |
| --- | --- |
| upload_sessions | 主键 id；`upload_key` 唯一（幂等 create）；status 枚举 CHECK；`expired_at` 默认 created_at + session_ttl |
| chunk_records | 联合主键 (session_id, chunk_index)；外键会话；sha256 非空 |
| file_records | 主键 id；`session_id` 唯一（一个会话至多一个文件）；sha256 非空 |
| upload_session_events | 主键 id；外键会话；from/to 枚举 |

索引：`upload_sessions(status, expired_at)` 部分索引（仅 created/uploading）；`chunk_records(session_id)`；`file_records(name, uploaded_at)`、`file_records(uploaded_at)`。

迁移命令：

```
go run ./cmd/uploader migrate up
go run ./cmd/uploader migrate status
go run ./cmd/uploader migrate down --steps 1   # 仅开发环境
```

生产环境单独执行迁移；服务启动只校验最低迁移版本，不自动执行 destructive down migration。

## 6. HTTP API

基础路径 `/api/v1`，响应带 `X-Request-ID`。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/v1/uploads | 创建上传会话（幂等，`upload_key`） |
| GET | /api/v1/uploads/{id} | 会话详情 / 断点续传位图（含缺失分片列表） |
| PUT | /api/v1/uploads/{id}/chunks/{index} | 上传分片（携带分片大小与哈希，幂等） |
| POST | /api/v1/uploads/{id}/complete | 校验并合并（幂等） |
| DELETE | /api/v1/uploads/{id} | 取消会话（aborted） |
| GET | /api/v1/files | 元数据查询（筛选 + 游标分页） |
| GET | /api/v1/files/{id} | 元数据详情 |
| GET | /api/v1/files/{id}/content | 流式下载（Range / ETag） |
| DELETE | /api/v1/files/{id} | 删除文件 |
| GET | /api/v1/healthz、/api/v1/readyz | 存活与就绪检查 |

状态码与错误约定：

- 会话创建成功 `201`；`upload_key` 重复返回 `200` 既有会话。
- 分片上传成功 `200`；分片过大/过小 `422`；会话不存在或已过期 `404/410`。
- complete 缺分片或分片校验失败 `422`；整体校验和不一致 `409`；并发合并 `409`（幂等可重试）。
- 统一错误体：`error.code`、`error.message`、`error.request_id`、`error.details`。

## 7. 后台任务

- `reaper`（过期回收）：周期扫描过期/悬挂会话与孤儿分片，advisory lock 防多实例重复，删除后写审计并上报指标。
- `merger`（异步合并）：从 `merging` 状态且 `claimed_at` 过期的会话中领取任务，执行 2.5 节合并流程；领取即更新 `claimed_at` 防并发。
- 容错：单任务失败不中断循环；连续失败的任务置 `failed` 并记录 `failure_reason`；两类 worker 均支持优雅停机。

## 8. 前端界面（简单）

`web/` 目录：`index.html` + `app.js` + `style.css`，通过 `go:embed` 编译进二进制，由 Go 服务在 `/` 提供静态页面，无任何构建依赖（原生 fetch）。

- 上传区：文件选择、分片大小选择（默认 8MB）、开始/暂停/继续/取消。
- 进度展示：整体进度条 + 分片网格（未传/传输中/已传/失败），失败分片自动重试。
- 断点续传：页面刷新后调用会话详情接口，自动跳过已上传分片继续。
- 结果区：完成后展示文件 ID、下载链接与整体 SHA-256。
- 文件列表：按名称/时间筛选、分页查询、下载、删除。

## 9. 安全与可观测性

- 配置：环境变量优先 + 可选 YAML；覆盖 DB URL、监听地址、存储根目录、会话 TTL、分片策略、worker 间隔、回收宽限期、Bearer token（可选）。
- 请求限制：单请求体不超过分片上限；上传总量受策略校验；分片写入受 `Content-Length` 双重约束。
- 日志：`log/slog` JSON 输出，字段含 request_id、session_id、file_id、chunk_index、worker、duration_ms；脱敏 Authorization。
- 指标：会话创建/完成/失败/过期计数、分片接收字节、合并耗时、回收释放字节、队列深度、worker 错误。
- 健康检查：healthz 仅进程存活；readyz 校验 DB ping、迁移版本与存储目录可写。

## 10. 测试、运行与验收

测试覆盖：会话状态机迁移、分片规则校验、SHA-256 计算与比对、位图/缺失分片计算、合并幂等、reaper 边界（过期/悬挂/孤儿）、仓储内存桩、HTTP 错误映射、并发 complete 行锁、续传位图。

最低运行与验证命令：

```
go mod download
go test ./...
go test -race ./...
go vet ./...
go run ./cmd/uploader migrate up
go run ./cmd/uploader serve
curl -fsS http://127.0.0.1:8080/api/v1/healthz
curl -fsS http://127.0.0.1:8080/api/v1/readyz
```

端到端必须证明：

1. 100 个分片全部上传后 complete 成功，文件 SHA-256 与客户端一致；
2. 中途中断后查询位图、只补传缺失分片，续传成功；
3. 缺分片 complete 返回 422，补传后重试成功；
4. 篡改一个分片后 complete 返回 409；
5. 置短 TTL 后 reaper 把过期会话置 expired 且磁盘目录被删除；
6. 并发 complete 只有一个成功，另一个幂等返回。

规模校验脚本：

```bash
files=$(find . -name '*.go' ! -name '*_test.go' | wc -l)
lines=$(find . -name '*.go' ! -name '*_test.go' -print0 | xargs -0 wc -l | tail -1 | awk '{print $1}')
test "$files" -gt 20 && test "$files" -lt 25
test "$lines" -gt 2000 && test "$lines" -lt 2200
```

## 11. 文件与代码规模计划

设计目标：**22 个非测试 Go 文件，合计约 2195 行**，落在 2001–2199 区间。

| # | 路径 | 职责 | 预估行数 |
| --- | --- | --- | --- |
| 1 | cmd/uploader/main.go | 入口、命令分发、依赖组装 | 105 |
| 2 | internal/config/config.go | 配置加载与校验 | 100 |
| 3 | internal/domain/session.go | 会话模型与状态机 | 130 |
| 4 | internal/domain/chunk.go | 分片模型与规则校验 | 95 |
| 5 | internal/domain/file.go | 文件记录模型与状态 | 90 |
| 6 | internal/domain/hash.go | SHA-256 计算与比对 | 80 |
| 7 | internal/domain/policy.go | 上传策略与领域错误 | 100 |
| 8 | internal/port/repository.go | 会话/分片/文件仓储接口 | 80 |
| 9 | internal/port/storage.go | 字节存储接口 | 60 |
| 10 | internal/port/clock.go | 时钟与 ID 生成接口 | 45 |
| 11 | internal/app/upload.go | 创建会话、写分片用例 | 145 |
| 12 | internal/app/resume.go | 续传位图与缺失计算 | 85 |
| 13 | internal/app/complete.go | 校验与合并编排 | 125 |
| 14 | internal/app/file.go | 元数据查询与删除用例 | 95 |
| 15 | internal/worker/reaper.go | 过期回收任务 | 95 |
| 16 | internal/worker/merger.go | 异步合并任务 | 90 |
| 17 | internal/store/postgres/db.go | 连接池、事务助手、迁移校验 | 80 |
| 18 | internal/store/postgres/session_store.go | 会话持久化 | 110 |
| 19 | internal/store/postgres/chunk_store.go | 分片持久化 | 100 |
| 20 | internal/store/postgres/file_store.go | 文件记录持久化 | 110 |
| 21 | internal/store/disk/blob.go | 磁盘字节存储实现 | 105 |
| 22 | internal/transport/http/server.go | 路由、中间件、DTO、错误映射、前端 embed | 170 |
| — | **合计** | | **2195** |

说明：实现阶段以脚本精确校准到 2001–2199 行；测试文件、`migrations/*.sql`、`web/*` 前端、README 与 go.mod/go.sum 均不计入。

## 12. 交付标准

交付物：Go 模块、版本化迁移、22 个分层源文件、测试代码、前端页面、运行配置、健康检查、结构化日志、核心指标、规模校验脚本与 README（含 API 示例）。

验收要求：断点续传正确且刷新/重启无损；分片与整体校验严格（篡改必拒）；合并幂等可靠；过期回收无泄漏且多实例不重复；元数据查询与下载完整；全部测试、竞态检查、静态检查与规模检查通过。
