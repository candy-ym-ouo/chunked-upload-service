const file=document.querySelector('#file'),out=document.querySelector('#out'),progress=document.querySelector('#progress');document.querySelector('#start').onclick=async()=>{const f=file.files[0];if(!f)return;const size=1024*1024,key=crypto.randomUUID();let r=await fetch('/api/v1/uploads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({upload_key:key,file_name:f.name,mime_type:f.type,total_size:f.size,chunk_size:size})});const s=await r.json();for(let i=0;i<s.chunk_count;i++){const b=f.slice(i*size,Math.min(f.size,(i+1)*size));await fetch(`/api/v1/uploads/${s.id}/chunks/${i}`,{method:'PUT',headers:{'X-Chunk-Size':String(b.size)},body:b});progress.value=(i+1)/s.chunk_count}const done=await fetch(`/api/v1/uploads/${s.id}/complete`,{method:'POST'});out.textContent=JSON.stringify(await done.json(),null,2)};
